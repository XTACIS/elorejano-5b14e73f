// Server-only: the original self-contained El Orejano page (2.6 MB, includes
// images, fonts and the runtime). Never imported from client code.
import siteHtml from "./elorejano-site.html?raw";

export { siteHtml };
